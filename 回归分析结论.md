# 回归分析结论

- 数据文件：`城镇调查失业率.xlsx`
- 原始月份数：35
- 多元回归建模样本量：27

## 相关分析、偏相关分析与距离分析

- 自动筛选出的高相关变量对为：`X1`（31个大城市城镇调查失业率）与 `X4`（全国城镇16—24岁劳动力失业率）。
- Pearson 相关系数 r = 0.9172，p = 0.0000。
- 一阶偏相关控制变量为 `X3`（全国城镇外来户籍劳动力失业率），偏相关系数 = 0.8507，p = 0.0000。
- 距离分析使用上述两变量构造欧氏距离矩阵，结果已写入 Excel。

## 一元回归

- 自变量：`y`（全国城镇调查失业率）
- 因变量：`X7`（企业就业人员周平均工作时间）
- 一元回归方程：`X7 = 55.6600 - 1.3750*y`
- 调整R² = 0.1747

## 逐步回归路径

- 第1步：加入 `X2`，当前变量组合为 `X2`，调整R² = 0.8050，AIC = -82.1659
- 第2步：加入 `X3`，当前变量组合为 `X2, X3`，调整R² = 0.9356，AIC = -111.1619
- 第3步：加入 `X5`，当前变量组合为 `X2, X3, X5`，调整R² = 0.9450，AIC = -114.5775

## 与因变量 y 的相关性

- `X2` 与 `y` 的相关系数为 0.9014
- `X1` 与 `y` 的相关系数为 0.6747
- `X6` 与 `y` 的相关系数为 0.5543
- `X4` 与 `y` 的相关系数为 0.5108
- `X3` 与 `y` 的相关系数为 0.4572
- `X5` 与 `y` 的相关系数为 0.2658
- `X7` 与 `y` 的相关系数为 -0.4816

## 模型比较结论

- 最优模型为 **模型3**。
- 自变量组合：`X2, X3, X5`
- 调整R² = 0.9450，AIC = -114.5775
- 回归方程：`y = 0.1763 + 0.7084*X2 + 0.3128*X3 - 0.0402*X5`

## 解释

- 相关分析部分不再手工指定 x 和 y，而是自动选择表中相关性最高的一对变量进行分析。
- 逐步回归部分仍以全国城镇调查失业率 y 为因变量，便于和你的实验主题保持一致。
- 最终模型解释力较强，但样本量有限，且未单独处理时间序列特征，结论更适合课程分析与结构解释。